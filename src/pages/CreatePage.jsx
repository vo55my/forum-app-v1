import ThreadInput from "../components/ThreadInput";

const CreatePage = () => {
  return (
    <div className="container">
      <h1 className="fw-bold my-5 text-center">Create New Thread</h1>
      <ThreadInput />
    </div>
  );
};

export default CreatePage;
